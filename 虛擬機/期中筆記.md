```
[terminology]
1. VMM (Virtual Machine Manager), also known as hypervisor
2. 
```
# benefit
1. protection
2. iolation
# feature
1. snapshot
	1. 可以 suspend (freeze)，接著再 resume
2. live migration
	1. benefit : improve resource management
	2. feature : move a running guest from one physical sever to another without interrupting its operation or active network connections
# build
1. although concept is useful, but difficult to implement
2. `technique` to implement virtualization
	1. trap and emulate
	2. binary translation
## trap and emulate
![[Pasted image 20260411131417.png]]
1. method : privileged instruction $\rightarrow$ error $\rightarrow$ cause trap to VMM $\rightarrow$ VMM gain control and execute (emulate) $\rightarrow$ return control to virtual machine
## binary translation
![[Pasted image 20260411133345.png]]
1. not have clean separation of privileged and non-privileged instruction
2. `special instruction`
3. easy concept, complex in implement
	```
	if guset VCPU in "user mode", guest run natively on physical CPU
	if guest VCPU in "kernel mode", guest belive running in kernel mode (detail : 待補上...)
	```
# isuue : memory management
## nested page tables (NPTs)
![[Pasted image 20260411133955.png]]
1. both for (1) trap-and-emulate (2) binary translation
2. Q : how can VMM keep page-table state both for guest, believe they are managing the page tables, and for VMM itself ?
# type of VM
## type 0
## type 1
## type 2
## paravirtualization (半虛擬化)